
  # Créer maquette selon document

  This is a code bundle for Créer maquette selon document. The original project is available at https://www.figma.com/design/eCTuQMr7NNDNePL0SXM7XZ/Cr%C3%A9er-maquette-selon-document.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  